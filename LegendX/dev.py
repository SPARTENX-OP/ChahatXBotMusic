from legendx modules import * 
clients profiles.pro
        if 
        legendpro=x{event}
                      event(0)
                            legend(deqsys).utils
                                     utils.loading.sever
                                         def plugins import dev.py($py)
                            pytgcalls(install.("pip install pytgcalls")
                            pyrogram(install.(:"pip install pyrogram , telethon ,telegramClients")
                                    loading-server(program:base if legend-server(import*{in}))/
                                     close
                                     class:10 loop
                                     
